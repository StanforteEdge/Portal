Use the page, extract components, and add the missing components, remove unneeded, don't write your own css, use existing (tailwind-css), to build our profile pages into @templates/pages/profile folder

- Take it one page after the other.
- Use strictly the components in those files, and if you need to use new components, copy from @Source @app.css 
- write jquery that send , and responds to the specified apis for each page (including the apis that are yet to be created as speciied in @api.md 

And clarification you need on this task?