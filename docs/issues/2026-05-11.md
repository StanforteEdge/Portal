# Roles and Permissions
- Our roles and permissions system keeps misbehaving. Right now, users with roles that does not have any payroll permissions, are seeing HR-Payrol menu. This should be a simple system. Menu items should be gated by permissions, pages should be gated by permissions, actions should be gated by permission. And users with roles that has that permission should be gated by the permissions their roles have. THis should be simple. When adding a new permission, seeding should be additive, as some permissions and roles may have been and modified. New pages also should simply be gated by the permissions that need to see them. There should be no reason why a user without a specific permission is seeing a page or a menu item.
- Some staff "Worked this cycle" in the Attendnace page is not updating. It is showing the same value always. 
- On Staff New Requests page. The different requests types are not fetching or displaying.
- In HR Employees Editing, there's no where to indicate Employee status