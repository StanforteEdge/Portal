1. Request Categories should have the save card width on New Request Page
2. Loan request should also have a limit to how much a staff can get. (There's a policy that informs that). But this limit, should also have a way to make a special request subject to approval, so maybe it should prompt users of the limit if a user exceeds, but user can still try to make a request that exceeds the limit and be approved. This also goes for other request like leave etc. Lets find a dynamic way to make this happen.
3. Where will the HR see the request to approve, apart from the Approvals page. Does the HR need a Loan page to see staff with loans, and see the list, approve etc, and set the limit.
AFter the Staff confirmed. It is asking the user to Retire. It is not a payment request but loadn, so the loan form and loan view has to be different. And if it is a money that is Loan, that the user has to pay back, not deducted from salary, how will he do it?
A user requested for a loan, it was disbusred and confirmed by the user, but it didn't appear in the hr's payroll, Loan tab
